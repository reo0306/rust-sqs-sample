[package]
name = "simple_notification"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-config = "1.8.12"
aws-sdk-sns = "1.93.0"

lambda_runtime = "1.0.1"
serde = "1"
serde_json = "1.0.149"
tokio = { version = "1", features = ["macros"] }

[dev-dependencies]
mockall = "0.14.0"

