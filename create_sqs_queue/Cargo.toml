[package]
name = "create_sqs_queue"
version = "0.1.0"
edition = "2024"

[dependencies]
aws-config = "1.8.18"
aws-sdk-sqs = "1.102.0"
tokio = { version = "1.53.1", features = ["rt", "rt-multi-thread", "macros"] }
