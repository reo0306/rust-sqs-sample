[package]
name = "mock_ddb"
version = "0.1.0"
edition = "2024"

[dependencies]
aws-config = "1.8.18"
aws-sdk-dynamodb = "1.116.0"
tokio = { version = "1.53.1", features = ["rt", "rt-multi-thread", "macros"] }

[dev-dependencies]
mockall = "0.15.0"

[features]
ministack = []